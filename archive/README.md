# archive/

Preserved historical outputs. **Not runtime product code.**

Nothing in this directory is imported by the canonical app (`artifacts/legal-luminaire`) or served as the production API.

Until folders are physically moved here (Phase 3), treat these locations as archived in place:

- `SUPPLEMENT/` — read-only agent snapshots, patches, and task log (`SUPPLEMENT/ARCHIVED.md`)
- `Attached_Assets/` — reference documents
- `output_100426/` — one-off generated filing bundles
- `OPERATION PROJECT TEST/` — tester corpora

Do not add new product features here. Do not delete these trees until they have been copied into `archive/` (or an external backup) first.
