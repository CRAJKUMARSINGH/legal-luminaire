# experiments/

Prototypes and lab code. **Not on the production path.**

Until trees are moved here, treat these locations as experiments in place:

- `artifacts/api-server` — Express API (FastAPI is canonical)
- `artifacts/mockup-sandbox` — UI mockup sandbox
- `ETERNAL_RESEARCH_CHILD` — research/lab daemon

Do not wire these into the SPA, Docker production compose, Netlify, or Vercel.
